package sokoban.domain;

/**
 * All game modes could be found here.
 */
public enum Mode {
    /**
     * Different game modes where the player could switch to.
     */
    PLAYERNAME, PLAY, MENU, CONTROLS, LEADERBOARD
}